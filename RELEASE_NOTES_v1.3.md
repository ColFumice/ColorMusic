# Neuro（demo）v1.3 更新说明

## 重大更新

- 新增录音、单次播放、循环播放和最多 13 个片段的混音台。
- 音频片段支持重命名、静音、音量、双向裁剪、试听、克隆、删除以及 WAV/MP3 导出。
- 新增样式与样式流管理，支持预览、重命名、载入、删除、导入导出以及定时切换。
- 新增手机和平板分辨率自动检测与横屏界面自适应。
- 优化多点触控提示、快速触摸发音延迟、波表音高、混合色音色合成和释放包络。
- Android 显示名称改为 `Neuro（demo）`，应用 ID 仍为 `com.colormusic.game`，以保持升级兼容。

## Android 与 MT 管理器

导出的录音和样式数据包保存在应用的 `ColorMusic` 目录。MT 管理器用户可通过“添加本地存储”添加本游戏，再进入 `android_data/files/ColorMusic` 管理和分享文件。完整设置步骤见 README。

## 兼容信息

- Android `arm64-v8a`
- 版本名：`1.3`
- 版本代码：`4`
