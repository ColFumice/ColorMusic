# Neuro（demo）v1.5 更新说明

## 音频编辑轨道重大更新

- 重写原混音台为左右分栏的“音频编辑轨道”：左侧管理录音源，右侧提供最多 13 条可滚动、缩放的时间线轨道。
- 支持把音频源拖入轨道，并对音频块执行选择、克隆、向左对齐、删除、颜色设置、长按拖动、节拍吸附和双击试听。
- 音频源支持音量、起止裁剪、颜色、重置、试听、克隆、导出、删除和长按名称重命名；源参数会同步到关联音频块。
- 轨道头支持滑动静音和双击独奏；播放中切换静音或独奏会立即更新对应轨道音量并保持时间同步。
- 新增时间标尺、可拖动时间戳、播放/停止、时间戳跟随、动画回到零刻度、左右栏收起以及轨道撤回功能。
- 新增音频块音量、音高和声相自动化曲线编辑，支持多点拟合、速度调整、裁剪、试听、撤回和重置。
- 支持将当前轨道编排导出为 WAV 或 MP3，并可在导出后直接查看文件目录。

## 节拍器与样式

- 新增节拍器开关，可设置拍号与 `20-320 BPM`；轨道强拍、次强拍和弱拍会使用不同线宽与刻度字号。
- 节拍器设置会随样式保存和恢复。
- 节拍器属于监听信号，不会写入录音文件。

## 音频与性能优化

- 重构 Android 原生实时合成命令队列，降低快速多指演奏时的主线程竞争、卡顿和持续音残留。
- 非正弦波改用带限波表并加入平滑频带切换，减少多指混合时的混叠与破音。
- 优化输出限制、效果器状态和音频尾音清理，降低高压演奏时的削波、异常反馈和卡死风险。
- 轨道网格采用视口外预加载、分块绘制、惯性滚动与延迟重绘，减少大时间线拖动和缩放时的卡顿。
- 修正时间戳在拖动、预加载刷新、跟随模式和惯性结束时跳动或闪烁的问题。

## 鼓与界面

- 新增沙槌、响葫芦、铃鼓、响棒、边击、通鼓、牛铃和邦戈 8 种特殊打击乐，总计内置 29 个可再分发鼓采样。
- 增加“浊厚底鼓”默认增益，并更新第三方采样来源与许可清单。
- 优化音频块、轨道头、屏幕边框、控制按钮、颜色面板和中英文默认名称。
- 更新中英文游玩说明，补充节拍器和音频编辑轨道操作。

## 兼容信息

- Android `arm64-v8a`
- 版本名：`1.5`
- 版本代码：`6`
- 应用 ID：`com.colormusic.game`（可从此前版本直接升级）

## 下载

- APK：`Neuro-demo-1.5-arm64-v8a.apk`
- SHA-256：`46410474296188589C854C10A3839A52D36A6529B071EB321B1CD6C21BB68D69`
- 本版本使用调试签名；如设备中已有不同签名的安装包，Android 可能要求先卸载旧版本。
