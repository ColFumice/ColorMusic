# Neuro（demo）v1.7 更新说明

## 文件管理重大更新

- 原“样式管理”重写为三栏式“文件管理”，统一管理样式、样式流、完整轨道编排和游戏导出的音频。
- 左上角“保存位置”可由玩家选择 `Neuro_Save` 所在位置；游戏会自动建立 `Neuro_Music`、`Neuro_Style`、`Neuro_Style_Flow` 和 `Neuro_Track` 四个子文件夹。
- 对应格式文件可直接放入上述子文件夹，并在重新打开文件管理后读取，无需再次导入或导出数据包。
- 样式与样式流保留预览、重命名、加载和删除；轨道支持保存全部编排、重命名、加载和删除。
- 音频列表仅显示游戏导出的 WAV/MP3，支持重命名、删除、WAV/MP3 互转，以及带进度拖动和已播放时长的试听。
- 各分类提供查看目录和清空列表操作；修复打开目录导致闪退的问题。

## 音频轨道与导出

- WAV/MP3 导出改为游戏内统一风格窗口，可自定义文件名、起始拍和结束拍。
- 导出会合成所选范围内的全部非空轨道，不再受轨道静音、独奏或音频启用状态影响。
- 新增全屏幕布导出进度：背景与原幕布保持比例，反色区域从左向右表示进度，完成后自动退出。
- 导出音频统一写入 `Neuro_Music`，成功后刷新文件管理音频列表，并正确恢复主界面“音频合成器已就绪”状态。
- 全屏轨道左上角新增文件管理入口，并调整按钮间距；修复音频块对齐时的停顿及连接节点未实时跟随的问题。
- 修复 WAV/MP3 导出闪退、导出按钮无响应和输入框文字未居中的问题。

## 动感节拍器与触屏反馈

- 节拍器开启时，强拍会让主界面全部网格线和屏幕边框从 3 倍线宽平滑恢复，次强拍从 1.5 倍恢复，动画持续一拍。
- 新增基于设备实际显示密度的 2.5 cm 触屏反馈范围：触点中心网格及边框最高为 3 倍线宽，并向外连续恢复。
- 触摸叠加层透明度从中心 100% 连续衰减至半径边缘 0%，与底层网格自然合成。
- 按下时快速渐强，松开后在一个半拍内缓慢恢复；连续点击会保留上一触点的独立退场动画。

## 界面、方向与提示

- “沉浸锁”开启时固定当前屏幕方向，不再随陀螺仪旋转；文件管理和音频编辑轨道固定为横屏。
- 修复部分横屏启动场景顺时针旋转 90° 的问题。
- 右上角菜单列表放大并下移，移除菜单圆形按钮白色边框，避免菜单与按钮重叠。
- 新增右下角成功提示动画，用于音频导出、样式/样式流保存、加载、清空、图片加载、网格/波表重置和节拍器设置等操作。
- 优化提示框颜色、透明度、边框粗细及进入/离开速度。
- 文件管理保存位置按钮改为黑底白字，并显示当前目录位置。
- 更新中英文游玩说明，加入文件管理、范围导出、动感节拍器和触屏网格反馈。

## 兼容信息

- Android `arm64-v8a`
- 版本名：`1.7`
- 版本代码：`8`
- 应用 ID：`com.colormusic.game`（可从此前版本直接升级）

## 下载

- APK：`Neuro-demo-1.7-arm64-v8a.apk`
- SHA-256：`853245BF87F98CB033C4CABF676393C3112683E4119D9B732EE55D6F8F9D8697`
- 本版本使用调试签名；如设备中已有不同签名的安装包，Android 可能要求先卸载旧版本。
