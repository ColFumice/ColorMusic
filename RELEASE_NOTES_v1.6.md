# Neuro（demo）v1.6 更新说明

## 音频编辑轨道

- 时间线改为“预加载但不销毁”：首次加载 200 拍，随后按 100 拍递增，并保留已经生成的网格、轨道横线和音频块。
- 修正纵向滚动边界、空轨道显示、时间轴位置、标尺数字与网格绑定，以及音频块和时间戳随惯性移动时的坐标同步。
- 时间戳可在没有音频块或后方没有音频块时继续行进、停止和追踪；修正竖线、倒三角、点击定位及收栏动画中的显示和图层问题。
- 修正清空轨道误清除网格与轨道头，以及复制或拖动到屏幕右侧时全部音频块异常跳移的问题。

## 裁剪、分割与自动化

- 磁吸开启时，裁剪范围按实际纵向网格单元吸附，同时允许首尾不足一拍的网格。
- 裁剪现在只删除划过的部分，并将剩余内容无缝对接为一个音频块；单击内部网格线可执行分割。
- 修正靠前片段裁剪方向错误、裁剪模式无法退出，以及裁剪后错误保留前段的问题。
- Android 实时播放与 WAV/MP3 导出均应用音量自动化曲线；曲线贴底时音频会真正静音。

## 收栏、连线与图层

- 音频栏可平滑缓入缓出地压缩为彩色连接节点，轨道网格、轨道头与时间戳同步移动。
- 新增音频栏到对应音频块的彩色连线；支持单块连接、音频栏全部连接和屏幕外自动隐藏。
- 全屏轨道模式保留静音按钮宽度的左侧区域，并提供还原、撤回与音频源节点按钮。
- 统一音频块、选中外框、轨道头、时间轴黑色条、时间戳与连线的图层顺序。

## 多选与交互

- 全屏轨道左上角新增多选按钮、33% 黑色遮罩、单击提示和实时圆角框选。
- 多选音频块支持整体长按移动、克隆、删除、左对齐和切换颜色；发生重叠时自动下移轨道。
- 优化多选切换、单击、框选和长按延迟；移动松手后直接落在最终位置，不再重建轨道或重复播放位移动画。
- 修正音频块切换选择时外框偶尔缺失，并保证选中外框位于其他音频块之上、时间轴黑色条之下。

## 界面与兼容性

- 默认屏幕方向改为横屏。
- 强拍数字使用粗体，对应网格线加粗；全屏多选、停止和撤回按钮的渲染状态更加稳定。
- 优化音频栏下拉设置、收回与还原动画，消除空白区域、闪现、抽动和首帧停顿。

## 兼容信息

- Android `arm64-v8a`
- 版本名：`1.6`
- 版本代码：`7`
- 应用 ID：`com.colormusic.game`（可从此前版本直接升级）

## 下载

- APK：`Neuro-demo-1.6-arm64-v8a.apk`
- SHA-256：`6EECA022882463C87FDE2BD048F6C70F4516BC77E8BF81599478CC5ED346130C`
- 本版本使用调试签名；如设备中已有不同签名的安装包，Android 可能要求先卸载旧版本。
